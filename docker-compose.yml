services:

  juice-shop:
    image: bkimminich/juice-shop
    container_name: juice-shop
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - vuln-lab

networks:
  vuln-lab:
    name: vuln-lab_default
