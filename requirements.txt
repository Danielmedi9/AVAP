# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# DEPENDENCIAS PYTHON DEL PROYECTO
# ─────────────────────────────────────────────────────────────────────────────
#
# ¿Por qué tan pocas dependencias?
# → El diseño del proyecto minimiza dependencias externas intencionalmente.
# → Nmap, Trivy y OWASP ZAP corren dentro de Docker: no necesitan instalación.
# → Chart.js se carga desde CDN en el HTML: no necesita instalación Python.
# → Solo necesitamos 'requests' para el polling HTTP de disponibilidad.
#
# Instalación:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# HTTP client — usado por utils/waiter.py para verificar disponibilidad del servicio
# Versión mínima 2.28 para soporte de timeout moderno
requests>=2.28.0
